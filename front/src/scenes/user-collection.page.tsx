import * as React from "react";
import { AppLayout } from "layout";
import { UserCollectionContainer } from "pods/user-collection";

export const UserCollectionPage = () => (
  <AppLayout>
    <UserCollectionContainer />
  </AppLayout>
);
